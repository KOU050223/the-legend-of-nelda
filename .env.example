# LiveKit Cloud の WebSocket URL。API secret は絶対にここへ入れないこと。
VITE_LIVEKIT_URL=wss://your-project.livekit.cloud

# 本番/発表用: Token Endpoint（POST で { roomName, playerId, role } を受け取り、
# { token, url } を返す。LiveKit API Secret は endpoint 側の secret にだけ置く）。
VITE_LIVEKIT_TOKEN_ENDPOINT=https://your-worker.example.com/api/livekit/token

# ローカル PoC 限定: LiveKit Cloud dashboard で発行した、短命で room/identity 固定の token。
# VITE_LIVEKIT_DEV_TOKEN=eyJ...
