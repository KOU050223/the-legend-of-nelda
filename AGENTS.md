# AGENTS.md

仕様・採用技術に関しては `docs/`を参照する

## PR作成時のフロントエンド画面変更

フロントエンドの画面に変更がある場合は、変更対象画面のスクリーンショットを撮影し、`gh` コマンド経由でPR本文に貼り付ける。

スクリーンショットなどの確認用画像は一時ファイルとして扱い、プロジェクト内にコミットしない。PR作成後は一時画像を削除し、Gitの変更対象に含まれていないことを確認する。
